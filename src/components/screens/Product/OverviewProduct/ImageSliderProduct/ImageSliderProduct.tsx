import { ProductImages, ProductImagesAttributes } from '../../../../../redux/catalog/catalog.types';
import './ImageSliderProduct.scss';

type PropsType = {
	images: ProductImages[];
};

export default function ImageSliderProduct({ images }: PropsType) {

	return <></>;
}
